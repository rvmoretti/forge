# Forge (plugin)

Spec-first engineering orchestration for Claude Code: CTO operating contract,
state CLI with machine-enforced gates, tiered worker agents, METHOD +
brownfield + domain-pack skills, generated dashboard.

Full documentation, install instructions, changelog and user manual live at
the **repository root**: see `../../README.md` and `docs/manual.html`.
Spec-to-implementation audit trail: `docs/TRACEABILITY.md`.
